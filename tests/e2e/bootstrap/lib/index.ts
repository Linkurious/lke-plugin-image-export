// import GraphService from './graphService';

export class Bootstrap {
  static async bootstrap({ nodes, edges }): Promise<void> {
    // const windowHeight = config.windowHeight ?? 1280;
    // const windowWidth = config.windowWidth ?? 720;
    // await GraphService.populateNodes();
  }
}
